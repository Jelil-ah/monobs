# Xcode / build artifacts
build/
DerivedData/
.build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
.swiftpm/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS filesystem noise
.DS_Store
._*

# Defense in depth (AD-15): real-host config must live OUTSIDE the repo tree
# (see docs/host-config.md). If one ever lands here by mistake, never commit it.
hosts.toml
*.hosts.toml
.monobs/
t-priv-denylist*
.t-priv-selftest.tmp/
.monobs-report-selftest.tmp/

# Artefacts de process BMAD (prompts, rapports d agents, logs de review).
# Local-only : contiennent des chemins de la machine de dev et ne doivent
# jamais atterrir dans un repo public (t-priv les refuserait).
bmad/
